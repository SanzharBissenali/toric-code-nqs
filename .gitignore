__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/
.venv/
wandb/
outputs/

# local archive of removed narrative/report material (Part 3 of repo cleanup)
_archive/

# LaTeX build artifacts (regenerable)
paper/*.pdf
*.aux
*.fls
*.fdb_latexmk
*.synctex.gz
*.out
*.toc
*.bbl
*.blg

# dead W&B CSV exports, editor backups, macOS screenshots
wandb_export_*.csv
*.old
Screenshot*.png

# raw NQS checkpoints — keep derived curve JSONs, never raw weights
*.mpack

# local full mirror of $PSCRATCH/tc_nqs pulled before NERSC maintenance
# (raw archive: weights + per-run JSONs + logs + captured cluster code state)
/data/

# generated plots (spec: dpi=300 savefig target)
/figures/

# external QMC benchmark codes (cloned, never committed; build script is force-added)
/external/

# packaging build artifacts
*.egg-info/
