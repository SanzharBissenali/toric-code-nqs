# Copy to $PSCRATCH/tc_nqs/phase3d/defaults.env (read by submit_nqs_gridinv.sh / submit_nqs_batch.sh at job start).
# phase3d speed levers (merged 14fee84, audited 2026-09-09). Applied at a job's FIRST start to EMPTY knobs only.
INV_IMPL=dense
COMPUTE_DTYPE=float32
